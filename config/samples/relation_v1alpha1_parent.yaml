apiVersion: relation.lambda.coffee/v1alpha1
kind: Parent
metadata:
  labels:
    app.kubernetes.io/name: cr-enqueuer
    app.kubernetes.io/managed-by: kustomize
  name: parent-sample
spec:
  # TODO(user): Add fields here
